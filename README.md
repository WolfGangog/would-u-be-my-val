# valpackage

This repository contains the files for a small static site.

- `combined.html` — main combined page
- `val.html` — secondary page

Hosting: This repo can be served via GitHub Pages. If a `gh-pages` branch (or `main`) contains an `index.html`, GitHub Pages will serve it at `https://<owner>.github.io/valpackage/`.

If you want me to push and enable Pages, confirm you've responded to the earlier prompts allowing me to push from this environment.
